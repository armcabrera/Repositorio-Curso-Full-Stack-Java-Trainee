/**************************************eliminar capacitacion***********/
function someFunction(event) {
	if (confirm(" Desea eliminar Capacitacion?")) {
	/* some code if YES */
	}
	else {
		/* some code if NO */
		if (event.preventDefault) event.preventDefault();
		else event.returnValue=false;
		}
}
/*****************************************eliminar profesional********/
function someFunction2(event) {
	if (confirm(" Desea eliminar Profesional?")) {
	/* some code if YES */
	}
	else {
		/* some code if NO */
		if (event.preventDefault) event.preventDefault();
		else event.returnValue=false;
		}
}
/*****************************************eliminar cliente********/
function someFunction3(event) {
	if (confirm(" Desea eliminar Cliente?")) {
	/* some code if YES */
	}
	else {
		/* some code if NO */
		if (event.preventDefault) event.preventDefault();
		else event.returnValue=false;
		}
}
/*****************************************eliminar Administrativo********/
function someFunction4(event) {
	if (confirm(" Desea eliminar Administrativo?")) {
	/* some code if YES */
	}
	else {
		/* some code if NO */
		if (event.preventDefault) event.preventDefault();
		else event.returnValue=false;
		}
}
/*****************************************eliminar accidente********/
function someFunction5(event) {
	if (confirm(" Desea eliminar Accidente?")) {
	/* some code if YES */
	}
	else {
		/* some code if NO */
		if (event.preventDefault) event.preventDefault();
		else event.returnValue=false;
		}
}
/*****************************************eliminar asesoria********/
function someFunction6(event) {
	if (confirm(" Desea eliminar Asesoria?")) {
	/* some code if YES */
	}
	else {
		/* some code if NO */
		if (event.preventDefault) event.preventDefault();
		else event.returnValue=false;
		}
}
/*****************************************eliminar asistentes********/
function someFunction7(event) {
	if (confirm(" Desea eliminar Asistente?")) {
	/* some code if YES */
	}
	else {
		/* some code if NO */
		if (event.preventDefault) event.preventDefault();
		else event.returnValue=false;
		}
}
/*****************************************eliminar chequeos********/
function someFunction8(event) {
	if (confirm(" Desea eliminar Chequeo?")) {
	/* some code if YES */
	}
	else {
		/* some code if NO */
		if (event.preventDefault) event.preventDefault();
		else event.returnValue=false;
		}
}
/*****************************************eliminar consulta********/
function someFunction9(event) {
	if (confirm(" Desea eliminar Consulta?")) {
	/* some code if YES */
	}
	else {
		/* some code if NO */
		if (event.preventDefault) event.preventDefault();
		else event.returnValue=false;
		}
}
/*****************************************eliminar pago********/
function someFunction10(event) {
	if (confirm(" Desea eliminar Pago?")) {
	/* some code if YES */
	}
	else {
		/* some code if NO */
		if (event.preventDefault) event.preventDefault();
		else event.returnValue=false;
		}
}